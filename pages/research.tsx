import { NextPage } from 'next';
import PublicationList from '../components/PublicationList';

const Research: NextPage<unknown> = () => {
    return (
        <>
            <PublicationList />
        </>
    );
};

export default Research;
